# DEFECTS — known defects, gaps & risks

_Phase 1 baseline (commit `be160d3`). Aggregated from the defects sweep (docs, audits, risk register) plus the critique pass that found surfaces missing from the structured sweeps._

**42 defects** (41 from the `be160d3` sweep + 1 post-baseline finding from the route-acceptance crawler, PR #22) — P0 **0** · P1 **22** · P2 **15** · P3 **5**. (No P0 in the sweep; the critique pass surfaces several latent security/trust-boundary items — see the last section.)

Severity: **P0** launch-blocking / data-loss now · **P1** must fix before controlled launch · **P2** fix during hardening · **P3** deferred / V2.

## P1 (22)

| Defect | Status | Source | Detail |
|--------|--------|--------|--------|
| `m2-m6-mock-first` | mock | `docs/MASTER-PLAN-V2.md` | P1 — M2 Finance/HR, M3 Sales/CRM, M4 Delivery, M5 Money, M6 Portal/Seams are all 'built, mock-first demo' only; the entire finance/sales/delivery/money/portal surface has no production acceptance. |
| `ai-layer-inert` | mock | `docs/MASTER-PLAN-V2.md` | P1 — product billed as AI OS but LLM_PROVIDER=mock in production, 13 agent instruction sets with no live model, Composio code with no key; lead-gen differentiator deferred out of V1 (Master Plan v2 exists to fix this, not yet executed). |
| `gap-live-auth-rls` | partial | `README.md` | P1 — README remaining gap: live Supabase Auth for portal magic-link + DB-enforced RLS for demo-mode flows (app-layer checks cover demo); portal go-live also blocked on transactional SMTP + SPF/DKIM/DMARC (CREDENTIALS-NEEDED #21). Staff SSO + 36-table RLS are live per M1 audit, portal auth is not. |
| `gap-prod-secrets` | partial | `README.md` | P1 — Vercel prod secrets missing for Composio, Xero, Apollo, Hunter, Bayzat, Chat webhook; config files ready but account creation/key provisioning is manual and outstanding. |
| `gap-xero-live` | mock | `README.md` | P1 — no live Xero OAuth token exchange and no durable webhook persistence (xero_invoice_mirror); all invoice posting is mock (mock-xero-inv-*). M1 audit: 'Xero: mock only'. |
| `gap-inngest-workers` | mock | `docs/AUTOMATIONS.md` | P1 — Inngest workers not wired: retainer month-start, T-48h shoot lock, SLA day-for-day exist only as callable tRPC/seams (README gap); M8 daily lead-gen, competitor scan, and M10 weekly report are built mock-first but activation is blocked on missing INNGEST_EVENT_KEY/SIGNING_KEY; 5-min GitHub cron cannot run long AI jobs. |
| `m1-uat-signoff-pending` | partial | `M1-COMPLETION-AUDIT.md` | P1 — current M1 closeout blocked on Dubai office acceptance script execution and written Ayham/Molham confirmation; real-user first-login UAT pending for all 21 provisioned employees. |
| `chat-webhook-leaked` | real | `M1-COMPLETION-AUDIT.md` | P1 security — the production Google Chat webhook credential was pasted into chat once; rotation + encrypted Vercel variable replacement is a named closeout item (also CREDENTIALS-NEEDED Tier 3 #12) and has not been done. |
| `module1-lead-to-cash-open` | partial | `M1-COMPLETION-AUDIT.md` | P1 commercial — old signed Module 1 (Lead-to-Cash) definition-of-done remains open/partial on every row: Asana replacement + source reconciliation open, Won totals unreconciled, no live learning loop, no live KPIs, no client sign-off; must not be represented as complete. |
| `cutover-checklist-open` | partial | `CUTOVER.md` | P1 — all 10 pre-cutover checklist items are unchecked: prod migrations sign-off, env/secrets, SSO + portal magic-link allowlist, Composio OAuth redirect URLs, keys in Keeper, Asana residual export reconciliation, signed role matrix (AM margin deny/payroll SoD/portal scopes), seam idempotency-key freeze list, backup/DR, rollback known-good. |
| `cutover-backup-dr` | unknown | `CUTOVER.md` | P1 data-loss — backup + DR item (daily dump, RTO < 24h documented) is unchecked; no evidence of tested restore anywhere in the docs; rollback plan assumes 'restore last daily dump' which is unverified. |
| `risk-capacity-overrun` | real | `docs/MASTER-PLAN-V2.md` | P1 — risk register F-1: capacity math runs 2–5x over per-milestone windows (2026-07-02 review); mitigations are scope cuts (M11 read-only, no graph DB/ML infra); next cut is M11 to V2, never M8. |
| `risk-hunter-dead` | real | `docs/MASTER-PLAN-V2.md` | P1 money — risk F-7: Hunter account dead and NeverBounce at 0 credits while the verified-email gate is a signed M3 payment trigger; keys requested from owner (no vendor swap allowed); escalation required before M8 week 1 if re-provisioning fails. |
| `risk-payment-mapping` | unknown | `docs/MASTER-PLAN-V2.md` | P1 commercial — mapping of M7–M12 to remaining milestone payments has no Ayham/Molham agreement; unresolved payment scope for all post-M6 work. |
| `risk-wrong-vercel-project` | real | `docs/MASTER-PLAN-V2.md` | P1 — duplicate Vercel projects (hrmny-os-web, hrmny-sales-growth in personal account) create wrong-project deploy risk; Phase 0.8 archive-consolidation not done; deploys stay manual until it is; canonical is hrmny-os under developer@hrmny.co team. |
| `credentials-tier1-3-missing` | partial | `docs/CREDENTIALS-NEEDED.md` | P1 — Phase 0 blocked: no OpenRouter, OpenAI, Anthropic, Apollo, Composio, or Inngest keys; no Vercel team token or Supabase access token for the canonical prod resources (agents cannot deploy/migrate the real prod); no n8n API key; Chat webhook unrotated. Tier 4 (Xero app, LinkedIn/Canva, ads tokens, Sentry, SMTP, Asana/Airtable access, Tejari) all outstanding. |
| `ledger-0057-renumber` | real | `docs/AGENT-WORKSTREAMS.md` | P1 — migration ledger: slot 0057 is RETIRED; the in-flight asana-feature-lab branch must renumber its migration to 0062+ on rebase or journal order will diverge from sorted filename order (enforced/failed by packages/db/src/migration-security.test.ts); collision breaks CI and prod migration ordering. |
| `asana-connection-unverified` | partial | `docs/asana-compatibility.md` | P1 — Asana/Composio bridge is configured in preview only: COMPOSIO_API_KEY exists solely in Vercel preview env, production has none, and the app database has no saved Composio/Asana connection; external Asana identity/workspace cannot be asserted, so import/sync/webhooks are unacceptable until a key is supplied and verified. |
| `bayzat-payroll-not-durable` | mock | `BAYZAT-REPLACEMENT-PLAN.md` | P1 — payroll status: 'Approval/SoD demo exists but is not durable payroll; full engine next'; shifts/timesheets/UAE payroll/expenses/loans are the next batch with the highest operational and cutover risk; WPS SIF, EOS, proration, payslips all unbuilt. |
| `bayzat-compliance-signoff` | unknown | `BAYZAT-REPLACEMENT-PLAN.md` | P1 compliance — UAE legal/payroll rules (MOHRE, WPS SIF, gratuity/EOS, visa/EID expiry, AED snapshots, bilingual docs) must be approved by hrmny's UAE HR/payroll owner before production calculations are relied upon; no signatory or approved policy set supplied yet. |
| `asana-cutover-reconciliation-open` | partial | `CUTOVER.md` | P1 — Asana residual export → OS task-board reconciliation (pre-cutover item 6) and the source-migration/reconciliation rows of the signed Module 1 DoD are open; Asana writes cannot be disabled and cutover cannot start until source data is reconciled and adoption evidence exists. |
| `portal-staff-soft-boundary` | real | `PR #22 (route-acceptance crawler design notes)` | Portal→staff route access is soft: a portal actor can load any (staff) route's shell chrome because auth.session is a public procedure and there is no page-level group guard; denial happens only at the tRPC data layer (portalStaffBoundary). Staff→portal direction hard-redirects. Hardening: add a route-group-level guard in the (staff) layout for actorType=portal. Source: route-acceptance crawler design notes (PR #22). |

## P2 (15)

| Defect | Status | Source | Detail |
|--------|--------|--------|--------|
| `todo-scan-clean-demo-store` | dev-only | `apps/web/src/server/trpc/work-management-router.ts` | P2 — grep for TODO/FIXME/HACK/XXX across apps+packages found zero genuine markers; only hit is DEMO_SECTION_TODO: hard-coded demo UUIDs (DEMO_PROJECT_ID, sections, Dev Partner/Dev AM rule owners) backing a module-level in-memory Map store (getDemoWork) inside a production router — demo scaffolding shipping in prod code, data lost on restart. |
| `gap-bank-rec-ui` | dead | `README.md` | P2 — Bank reconciliation queue (Module C) UI does not exist at all. |
| `gap-rate-card-calendar` | partial | `README.md` | P2 — full rate-card workbook and DATE-FIRST calendar depth not built. |
| `m1-manual-deploys` | partial | `M1-COMPLETION-AUDIT.md` | P2 — accepted exception: no Vercel/GitHub link under current account arrangement, production deployment is manual; no staging environment (optional follow-up). Compounds the wrong-project deploy risk. |
| `llm-cost-metering-deferred` | partial | `M1-COMPLETION-AUDIT.md` | P2 — AED spend-cap convention configured but paid-provider cost metering intentionally deferred until a paid LLM is enabled; cap enforcement in packages/ai/src/provider.ts unverified against live spend (Phase 0.1 verification step). |
| `risk-bayzat-no-api` | partial | `docs/MASTER-PLAN-V2.md` | P2 — risk F-2: Bayzat has no public API; CSV/XLSX import is the permanent fallback and the HR mirror can silently go stale between manual exports. |
| `risk-llm-spend` | real | `docs/MASTER-PLAN-V2.md` | P2 — LLM spend-runaway risk; mitigation designed (AED 1,500/mo hard cap + per-agent sub-budgets + agent_runs cost rows) but agent_runs table/migration (slot 0058) and live-cap enforcement are not yet built/verified. |
| `risk-outreach-reputation` | real | `docs/MASTER-PLAN-V2.md` | P2 — reputational risk of autonomous outreach; HITL required on every external send through M12; removing the human gated on >=4 weeks of measured reply-classification precision (no such measurement exists yet). |
| `risk-n8n-ownership` | partial | `docs/AUTOMATIONS.md` | P2 — hrmny.app.n8n.cloud API key/ownership unconfirmed; the two built workflows (lead-source webhook ingestion, ops-alert fan-out) are validated only in Ahmad's personal STAGING tenant and exported to docs/automations/n8n/; tenant import, pin-test, activation, and N8N_ALLOW_PRODUCTION_TRIGGER flip all pending; workflow #3 (nurture) only planned. |
| `ledger-inert-leadgen-stores` | mock | `docs/AGENT-WORKSTREAMS.md` | P2 — orchestrator ruling: M8 outreach/competitor stores are in-memory (merged-but-inert); migrations 0059/0060 (outreach_items, lead_intel incl competitor_findings) are the durable schema-of-record but the live Postgres path (drizzle tables + withDb swap) has not landed — data written today does not persist. |
| `ledger-shared-worktree` | real | `docs/AGENT-WORKSTREAMS.md` | P2 process — the local worktree is shared across parallel agent sessions; standing rules (verify git branch --show-current before every commit, push immediately) exist because branch-switching races have previously stranded work. |
| `asana-catalog-beta` | partial | `docs/asana-compatibility.md` | P2 — feature catalogue (apps/web/src/features/catalog.ts): 131 capabilities all have a usable surface but 99 remain beta and only 32 available; beta is explicitly not production acceptance; 100% parity goal open until live Composio/Asana verification + client acceptance at production scale. |
| `asana-acceptance-gaps` | partial | `docs/asana-compatibility.md` | P2 — named beta acceptance gaps: live Gmail send-scope acceptance for form receipts; closed-app server push, focus-timer notification suppression, offline mutation (PWA); collaborative status-template drafts/attachments/sharing/AI-in-editor; real project auth configs + acceptance tests for each of the 14 Composio providers; vision-capable model for Smart Chat; synonym-only retrieval case; full-workspace portfolio import requires an Asana service account; pre-scope-expansion connections may need reconnection. |
| `bayzat-tenant-access-missing` | unknown | `BAYZAT-REPLACEMENT-PLAN.md` | P2 — production acceptance blocked on client inputs: read-only Bayzat tenant access or full screen export, complete data exports (24 months payroll/SIF etc), provider contract list, anonymised acceptance examples, and named Dubai testers — none supplied; parity with private/plan-specific Bayzat features unverifiable until then. |
| `bayzat-provider-dependent` | dead | `BAYZAT-REPLACEMENT-PLAN.md` | P2 — regulated capabilities (banking/cards/FX, insurance brokerage, WPS submission, biometrics, telemedicine/wellness) are provider-dependent and no provider has a named owner, live credentials, failure/retry procedure, or reconciliation report — all cutover gates for these are unmet. |

## P3 (5)

| Defect | Status | Source | Detail |
|--------|--------|--------|--------|
| `gap-ads-analytics-v2` | dead | `README.md` | P3 — deep Meta/Google/TikTok ads analytics + full marketing automation deliberately parked to V2 (M11 cut to read-only-first per risk register). |
| `gap-social-publish` | dead | `README.md` | P3 — one-channel social publish MVP / Airtable wrap listed as optional, not built (restored as M9 scope in Master Plan v2, not yet delivered). |
| `gap-uae-residency` | partial | `README.md` | P3 — UAE data-residency migration path documented only; Supabase remains in Singapore (accepted exception, revisit in ownership register). |
| `risk-netlify-personal` | real | `docs/MASTER-PLAN-V2.md` | P3 — hrmny-os-docs Netlify site lives on Ahmad's personal account; ownership transfer decision open (bus-factor/ownership risk). |
| `ledger-unsubscribe-no-stage` | partial | `docs/AGENT-WORKSTREAMS.md` | P3 — reply-intent ruling: 'unsubscribe' records no deal-stage move (no lost/disqualified stage exists; only moveDealStage is consumable) — documented calibration knob in reply-intent.ts; unsubscribes will not automatically stop pipelines. |

## Critique pass — inventory gaps & latent defects (19)

The critique pass audited the sweeps for blind spots. Some entries are **latent defects** (security / trust-boundary / hygiene) that no severity was assigned in the sweep but should be triaged; others are **coverage gaps** — real surfaces that no sweep dimension catalogued.

| Item | Kind | Status | Finding |
|------|------|--------|---------|
| `api-scim-v2-endpoints` | api-route-handler | real | 9 SCIM 2.0 route files (Users, Groups, Schemas, ResourceTypes, ServiceProviderConfig + [id] variants) backed by 850-line server/scim.ts with bearer auth via enterprise-identity.ts; absent from the 638-item api sweep which counts only tRPC procedures (verified 637 .query/.mutation) |
| `api-mcp-work-endpoint` | api-route-handler | real | 366-line MCP JSON-RPC endpoint exposing 11 work tools (list/create/update/complete/archive projects+tasks, comments) with scope-checked work-api token auth; in no sweep dimension |
| `api-work-v1-rest` | api-route-handler | real | Public REST catch-all: 12 endpoints across GET/POST/PATCH/DELETE (projects, tasks, comments, complete, archive) with scoped token auth via work-api.ts (696 lines), delegating to tRPC caller; missing from api sweep |
| `api-work-sandbox-environment` | api-route-handler | dev-only | Sandbox DB bootstrap/reset endpoint (bootstrapSandboxDatabase/resetSandboxDatabase) guarded by sandboxRequestAuthorized; destructive reset surface not in mock-surfaces sweep |
| `api-asana-webhook-receiver` | webhook | real | Token-addressed Asana webhook ingress delegating to server/asana-webhooks.ts (has tests); not listed under integrations or jobs sweeps |
| `api-n8n-webhook-receiver` | webhook | partial | n8n callback ingress whose signature verification is an explicit stub: accepts unsigned requests when N8N_WEBHOOK_SECRET unset and uses non-constant-time compare; trust-boundary defect absent from defects sweep |
| `desk-site-app` | secondary-app | mock | Separate pnpm-workspace Next.js app 'hrmny-os-desk' deployed to Vercel production (per _deploy-meta.json) with 3 routes (/, /crm, /portal) serving M1 status board + seeded CRM demo (lib/crm-seed.ts); entirely absent from the 69-route sweep |
| `desk-site-deploy-metadata` | artifact | dev-only | Committed Vercel deploy payload/meta/args JSONs (_deploy-meta, _deploy-payload, _mcp-deploy-args) exposing teamId and full file manifest; repo-hygiene finding in no sweep |
| `feature-catalog-entries` | catalogue | real | 132-entry code-owned FEATURE_CATALOG driving Feature Lab and per-route gating (protected/defaultEnabled/availability, sources native\|bayzat\|asana\|integration); api sweep covers admin.features.* procedures but the 132 entries themselves are uninventoried |
| `gate-registry` | catalogue | real | packages/gate business-rule surface: 10 gate modules (brief, calendar, deal, demo-confirm, employee, invoice, marketing, payroll, task, vat) + transition engine and bootstrap, unit-tested; gate enforcement not itemised in any sweep beyond the /gate demo page |
| `ai-agent-registry` | catalogue | partial | AGENT_REGISTRY with ~15 agent definitions (canva, creative+subagents, midjourney, higgsfield, hr, finance-assist, outreach-draft, portal-concierge, research, ticket-assist, delivery-qc, error-monitor, automation-orchestrator) plus evals/memory; defects sweep has only aggregate ai-layer-inert, no per-agent inventory |
| `permissions-matrix` | permissions | unknown | permission_policy table (schema table 23) exists but no sweep dimension catalogues the role-to-permission matrix or verifies per-route enforcement; permissions acceptance dimension missing |
| `e2e-coverage-gap` | test-coverage | partial | The only Playwright spec in the repo: smoke of 5 pages + 1 gate-block assertion vs 69 routes/638 procedures, and it relies on x-dev-role persona header; e2e coverage gap absent from defects sweep despite CI running an e2e job |
| `lint-stub` | defect | dead | Web app lint script is 'echo "lint stub"' and ci.yml has no lint step, so root pnpm lint passes without linting the entire app; not in defects/dead-controls sweeps |
| `root-package-scripts` | build-pipeline | real | Turbo-driven scripts (dev/build/test/typecheck/e2e/db:generate/migrate/studio) plus turbo.json env allowlist naming prod secrets (CRON_SECRET, SUPABASE_SECRET_KEY, COMPOSIO_API_KEY, LLM_MONTHLY_CAP_AED); package-scripts/pipeline dimension absent from sweeps |
| `ops-scripts` | script | real | Two ad-hoc ops scripts (check-db.mjs, fix-db-url-to-pooler.mjs) that hand-parse apps/web/.env.local and rewrite DATABASE_URL to the pooler; operational surface in no sweep |
| `supabase-config-stub` | infra-config | dev-only | Self-described 'local/link stub' with localhost-only ports/site_url and no linked prod project; env-separation evidence relevant to Phase 2 not catalogued in the database dimension |
| `n8n-workflow-blueprints` | automation | unknown | Exported n8n workflow JSONs (lead-source-webhook-ingestion.json, ops-alert-fanout.json) + README pairing with the /api/webhooks/n8n ingress; deployable blueprints absent from jobs/integrations sweeps (n8n-agent may hold these, unverified in inventory) |
| `no-edge-middleware` | defect | unknown | No middleware.ts exists anywhere in apps/web, so there is no edge-level auth guard and every route/handler depends on per-handler checks; structural fact underlying gap-live-auth-rls but not itemised as its own control |

**Latent defects worth triaging to P1/P2 in Phase 2/7:**

- `api-n8n-webhook-receiver` / `n8n-webhook-stub-verifier` — webhook signature verification is a stub (accepts unsigned when secret unset; non-constant-time compare). Trust-boundary defect.
- `no-edge-middleware` — no `middleware.ts`; every route depends on per-handler checks. Structural auth risk underlying `gap-live-auth-rls`.
- `lint-stub` — web app `lint` script is `echo "lint stub"` and CI has no lint step; the whole app is effectively unlinted.
- `desk-site-deploy-metadata` — committed Vercel deploy payloads expose `teamId` + full file manifest (repo hygiene).
- `e2e-coverage-gap` — the only Playwright spec smokes 5 pages/1 assertion against 69 routes / 637 procedures, and relies on a dev-role persona header.
