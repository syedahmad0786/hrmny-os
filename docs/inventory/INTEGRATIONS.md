# INTEGRATIONS — external services & scheduled work

_Phase 1 baseline (commit `be160d3`). Third-party integration adapters + every job/cron/automation._

## Integrations — live-readiness

**20 integration adapters** — real **3** · partial **8** · mock **8** · dead **1**

`real` = live production path · `partial` = real path + silent mock/memory fallback · `mock` = mock/demo-only, no real backing · `dev-only` = dev/demo scaffolding · `dead` = unreachable / no effect

Almost every adapter follows the same pattern: a mock/stub default, with a real client gated behind a `*_MODE=live` flag **and** an API key. "Live-ready" below means the real client is implemented and only a provisioned key/env is missing; "shell" means the live branch exists but throws / is unimplemented.

| Integration | Status | File | Live gate / readiness |
|-------------|--------|------|----------------------|
| `int-xero` | mock | `packages/integrations/src/xero/index.ts` | Default mock (XERO_MODE, defaults mock); live gated by XERO_MODE=live + XERO_CLIENT_ID/XERO_CLIENT_SECRET (+XERO_REDIRECT_URI) but live is a shell: getAuthorizeUrl builds a real OAuth URL while exchangeCode/createInvoice/createJournal all throw 'not wired' and disburse always throws by design — needs xero-node OAuth token exchange + invoice/journal POST wiring plus Keeper keys. |
| `int-bayzat` | partial | `packages/integrations/src/bayzat/index.ts` | CSV import path is real (parseBayzatCsv into in-memory mirror, source default csv); 'api' source gated by BAYZAT_SOURCE=api or BAYZAT_API_KEY presence only validates the key exists — there is zero Bayzat HTTP client, listEmployees just returns the mirror, so live API mode is unimplemented. |
| `int-composio-stub` | mock | `packages/integrations/src/composio/index.ts` | createComposioStub always stub: startOAuth returns fake redirect, status always disconnected, sendAfterApproval fakes gmail send / linkedin copy_draft with stub ids — no live sendAfterApproval implementation exists anywhere, so HITL outreach send (leadgen-router) never actually sends email. |
| `int-composio-live` | real | `packages/integrations/src/composio/live.ts` | createComposioLive is a real @composio/core SDK + REST v3.1 client (toolkits, authorize, connect links, connected accounts, disconnect, proxy) requiring an apiKey argument (callers read COMPOSIO_API_KEY); executeTool is allowlisted to 5 read-only search slugs (OneDrive/Outlook/Slack/Teams/Jira) — no write-tool execution and no factory/env-mode switch, callers must construct it. |
| `int-apollo` | partial | `packages/integrations/src/apollo/index.ts` | Default mock; APOLLO_MODE=live + APOLLO_API_KEY switches to a fully implemented live REST client (people/match, mixed_companies/search) that fail-louds without key; also wired live per-employee via Supabase Vault key in apps/web/src/server/trpc/m3-routers.ts apolloFor() — missing only a provisioned key/env to be live by default. |
| `int-apollo-leadsource` | partial | `packages/integrations/src/apollo/leadsource.ts` | M8 LeadSourceAdapter (contracts.ts) implementation: deterministic mock default, live Apollo REST (mixed_people/search + people/match) gated by APOLLO_MODE=live + APOLLO_API_KEY; consumed by leadgen-router and inngest leadgen-daily — live-ready except no key/env set. |
| `int-hunter` | partial | `packages/integrations/src/hunter/index.ts` | Default mock (heuristic verdicts); HUNTER_MODE=live + HUNTER_API_KEY switches to implemented live GET /v2/email-verifier; also wired live per-employee via Vault key in m3-routers.ts hunterFor() — missing only a key to go live. |
| `int-email-verification` | partial | `packages/integrations/src/hunter/verification.ts` | M8 EmailVerificationAdapter: mock default, live Hunter (HUNTER_API_KEY) and live NeverBounce (NEVERBOUNCE_API_KEY) both implemented; quirk: mode for BOTH providers keys off HUNTER_MODE=live, so NeverBounce can never go live without also setting HUNTER_MODE — verdict gates a payment-trigger deal transition. |
| `int-n8n` | partial | `packages/integrations/src/n8n/client.ts` | Mock default with hardcoded 6-workflow list; live REST (health/listWorkflows/executions) gated by N8N_MODE=live + N8N_API_KEY (silently downgrades to mock if key missing); webhook triggers additionally gated by N8N_ALLOW_PRODUCTION_TRIGGER, and N8N_WEBHOOK_DEAL_WON/TICKET_CREATED/ERROR_ALERT/TICKET_TRIAGE/OUTREACH_DRAFT URL overrides fire real POSTs without API key even in mock mode; base URL N8N_BASE_URL default hrmny.app.n8n.cloud; consumed by automation-router. |
| `int-asana` | real | `packages/integrations/src/asana/index.ts` | Full read/write Asana API adapter with two live transports — createAsanaDirect (requires PAT accessToken argument, no env) and createAsanaViaComposio (requires COMPOSIO_API_KEY + an ACTIVE Composio asana connected account, resolved by connections-router getVerifiedAsanaConnection); no mock variant exists, so it is dark until Composio/asana is connected. |
| `int-regulated-wps` | mock | `packages/integrations/src/regulated/index.ts` | WPS SIF submission adapter: mock returns accepted stub submissions; live mode has no env vars — it requires an explicitly injected licensed provider implementation (config.provider) that does not exist anywhere in the repo, and nothing in apps/web consumes the factory. |
| `int-regulated-cards` | mock | `packages/integrations/src/regulated/index.ts` | Corporate cards adapter: mock returns one stub card + txn; live requires injected licensed provider (none exists); no app consumer of createCorporateCardsAdapter outside package tests. |
| `int-regulated-insurance` | mock | `packages/integrations/src/regulated/index.ts` | Insurance policies/endorsements adapter: mock-only stubs; live requires injected licensed provider (none exists); unconsumed by apps/web. |
| `int-regulated-biometric` | mock | `packages/integrations/src/regulated/index.ts` | Biometric attendance adapter: mock device + one canned event; live requires injected licensed provider (none exists); unconsumed by apps/web; raw biometric templates deliberately excluded. |
| `int-storage` | real | `packages/integrations/src/storage/index.ts` | ObjectStore: createSupabaseObjectStore is a real Supabase Storage upload/signedUrl/remove implementation (client + bucket injected by demo-store.ts); createMemoryObjectStore is the in-memory dev fallback used when Supabase is absent — readiness depends on Supabase env, no adapter-level env vars. |
| `int-contract-social-publish` | mock | `packages/integrations/src/contracts.ts` | SocialPublishAdapter (M9) contract has no implementation in the package; the only implementation is createSocialPublishStub in apps/web/src/server/campaigns/repository.ts (mode mock, fake externalIds), fired from inside the approved→published campaign gate — no live LinkedIn/IG/FB/X publisher, no connection wiring. |
| `int-contract-ads-insights` | dead | `packages/integrations/src/contracts.ts` | AdsInsightsAdapter (M11 Meta/Google read-only spend ingestion) is a frozen interface with zero implementations and zero consumers in the repo (only contracts.ts + docs/specs/README.md) — pure forward contract. |
| `int-connections-router` | partial | `apps/web/src/server/trpc/connections-router.ts` | Real: saveApiKey (apollo/hunter/bayzat → Supabase Vault), getEmployeeIntegrationSecret decrypt, Google Workspace OAuth save + auto token refresh (GOOGLE_OAUTH_CLIENT_ID/SECRET, @hrmny.co-only), Composio work-app/managed-toolkit connect/disconnect gated by COMPOSIO_API_KEY + feature flags + connected-app policy; mock: startOAuth/completeOAuth/status route through createComposioStub and demo-store (completeOAuth refuses when DB present), canvaListDesigns returns hardcoded stub designs, canva/linkedin catalog entries marked ready:false. |
| `int-connections-page` | partial | `apps/web/src/app/(staff)/settings/connections/page.tsx` | Staff Connections UI wired to connections-router: real API-key save, Google Workspace OAuth via Supabase signInWithOAuth (gmail/calendar/drive/sheets scopes, hd=hrmny.co), real Composio work-app + managed-toolkit connect flows with 3s polling; the generic startOAuth path for gmail/calendar/canva/linkedin still hits the Composio stub (fake redirect). |
| `int-connection-health` | mock | `apps/web/src/app/(staff)/settings/connections/connection-health.tsx` | Connection-health card grid is entirely hardcoded MOCK_PROVIDERS (composio/apollo/hunter/xero/n8n) with fabricated statuses and timestamps; Connect/Disconnect buttons only flip local React state and 'Change' is a no-op — file comment says replace with a connections.health probe when the v2 router lands (M7). |

> Secrets/keys blocking go-live are tracked as defects (`gap-prod-secrets`, `credentials-tier1-3-missing`, `risk-hunter-dead`) in [DEFECTS.md](./DEFECTS.md).

## Jobs, crons & automations

**20 jobs/automations** — real **11** · partial **4** · mock **1** · dev-only **3** · dead **1**

The only live scheduler is the durable `scheduled_job` queue drained by `GET /api/cron/jobs`, curled every 5 min by a GitHub Actions cron. Inngest workers are scaffolded but **the `inngest` package is intentionally not a dependency**, so nothing fires; n8n workflows are exported but not imported into the hrmny tenant.

| Job / automation | Status | File | Note |
|------------------|--------|------|------|
| `cron-jobs-dispatcher` | real | `apps/web/src/app/api/cron/jobs/route.ts` | GET dispatcher for the durable scheduled_job queue: Bearer CRON_SECRET auth, unlocks stale running jobs (>10min), claims up to 20 due rows FOR UPDATE SKIP LOCKED, runs 6 job kinds, retries <3 attempts with 5-min backoff, emits job_lag health signal, then runs webhook-delivery + AI-run-cleanup sweepers; live (curled by scheduler.yml every 5 min). |
| `gha-scheduler` | real | `.github/workflows/scheduler.yml` | GitHub Actions cron */5 min + workflow_dispatch; curls https://hrmny-os.vercel.app/api/cron/jobs with HRMNY_CRON_SECRET secret; AUTOMATIONS.md marks it live with prod alert evidence; prod URL hard-coded (no env separation). |
| `gha-ci` | real | `.github/workflows/ci.yml` | Push/PR trigger: verify job (typecheck, test, build) plus e2e job (Playwright chromium against a prod next start on :3000); merge arbiter, runs on every PR. |
| `gha-nightly-eval` | partial | `.github/workflows/nightly-eval.yml` | Cron 01:30 UTC + workflow_dispatch; runs @hrmny/ai agent eval harness with LLM_PROVIDER=mock unless the OPENROUTER_API_KEY secret exists (then flips to openrouter, AED 1500 cap); tested in mock mode only per AUTOMATIONS.md. |
| `job-kind-health-signal` | real | `apps/web/src/server/trpc/root.ts` | scheduled_job kind=health_signal: enqueued by the ops tRPC mutation (root.ts ~L269) with a delay; dispatcher emits the health signal via emitHealthSignal; one-shot, real DB-backed. |
| `job-kind-asana-sync` | real | `apps/web/src/server/asana-sync.ts` | scheduled_job kind=asana_sync (job_key asana-sync:{workspaceGid}): recurring workspace sync + webhook refresh; enqueued by asana-migration-router.ts:286 and re-enqueued by the Asana webhook ingress (asana-webhooks.ts:429, route /api/asana/webhooks/[token]); gated on feature flag asana.sync and a verified live Asana connection — no-ops (ok:disabled) when the flag is off. |
| `job-kind-work-ai-studio` | real | `apps/web/src/server/work-ai-studio.ts` | scheduled_job kind=work_ai_studio: runs AI Studio workflow executions; event-enqueued (work-ai-studio-events.ts:32) and self-rescheduling recurring schedule (ai-studio-schedule:{workflowId}, default 5 min); execution real, AI output quality depends on LLM provider (mock without keys). |
| `job-kind-work-ai-teammate` | real | `apps/web/src/server/work-ai-teammates.ts` | scheduled_job kind=work_ai_teammate: teammate runs on assignment/mention/rule triggers (work-ai-teammate-events.ts:76,109) plus self-enqueued follow_up jobs (work-ai-teammates.ts:1504); dedup via job_key on-conflict-do-nothing; one-shot per event. |
| `job-kind-work-form-receipt` | real | `apps/web/src/server/work-form-receipts.ts` | scheduled_job kind=work_form_receipt (job_key work-form-receipt:{submissionId}): enqueued at form submission (work-management-router.ts:4979); dispatcher runs runWorkFormReceiptJob to produce the submission receipt; one-shot. |
| `job-kind-work-rule` | real | `apps/web/src/server/trpc/work-management-router.ts` | scheduled_job kind=work_rule (job_key work-rule-schedule:{ruleId}): recurring scheduled work-rule runs (default 15 min reschedule); enqueued on rule create/update/enable (work-management-router.ts:10276,10410,10550,11952); dispatcher calls runScheduledWorkRuleJob. |
| `sweep-work-webhook-delivery` | real | `apps/web/src/server/work-api.ts` | deliverPendingWorkWebhooks sweeper: runs on every /api/cron/jobs tick (not a queued job); delivers pending Work API outbound webhook deliveries; count returned in the cron response. |
| `sweep-expired-ai-runs` | real | `apps/web/src/server/work-ai.ts` | cleanupExpiredWorkAiRuns sweeper: runs on every /api/cron/jobs tick; expires stale Work AI runs; count returned in the cron response. |
| `inngest-leadgen-daily` | dev-only | `apps/web/src/server/inngest/leadgen-daily.ts` | Inngest scaffold for daily lead-gen at 06:00 Asia/Dubai (LEADGEN_DAILY const): the inngest package is intentionally NOT a dependency, no client/function registered, so the schedule never fires; blocked on INNGEST_EVENT_KEY/SIGNING_KEY; pipeline is only runnable via manual tRPC leadgen.runDailyPipeline with mock adapters (apollo_mock/hunter mock unless APOLLO_MODE/HUNTER mode=live). |
| `inngest-competitor-scan` | partial | `apps/web/src/server/leadgen/competitor-scan.ts` | AUTOMATIONS.md lists competitor scan as an M8 Inngest job blocked on INNGEST_* keys, but no Inngest scaffold exists — only the manual tRPC mutation leadgen.competitor.scan (leadgen-router.ts:267); no scheduled trigger anywhere. |
| `inngest-weekly-agency-report` | partial | `apps/web/src/server/analytics/report.ts` | M10 weekly agency report: assembly + deterministic mock-provider narrative built and exposed as on-demand tRPC analytics.weeklyReport (analytics-router.ts:77); AUTOMATIONS.md says scheduling blocked on INNGEST_* keys — no schedule or Inngest scaffold exists. |
| `n8n-lead-source-ingestion` | dev-only | `docs/automations/n8n/lead-source-webhook-ingestion.json` | n8n export (active:false, hrmny-STAGING prefix): POST /webhook/hrmny-lead-inbound → normalize → 400 if no email → forward to {HRMNY_OS_BASE_URL}/api/inbound/lead with X-Webhook-Secret, Google Chat alert on failure; validated with pinned data in Ahmad's personal staging tenant; NOT imported to the hrmny tenant (no API key), and the target route /api/inbound/lead does not exist in the app at this baseline. |
| `n8n-ops-alert-fanout` | dev-only | `docs/automations/n8n/ops-alert-fanout.json` | n8n export (active:false, hrmny-STAGING prefix): POST /webhook/hrmny-ops-alert → build Google Chat cardsV2 (severity/source/message) → POST to GOOGLE_CHAT_WEBHOOK_URL; validated in staging with pinned HTTP; tenant import blocked on hrmny n8n Cloud API key + env vars; nothing in the OS currently posts to it. |
| `n8n-nurture-sequence` | dead | `docs/AUTOMATIONS.md` | Workflow #3 in the n8n inventory — client-facing nurture sequence trigger (HITL-gated, consumes M8 send infra) — is 'planned' only; no export, no code. |
| `n8n-inbound-callback-route` | mock | `apps/web/src/app/api/webhooks/n8n/route.ts` | POST /api/webhooks/n8n callback ingress from n8n Cloud: signature verification is an explicit stub (accepts anything when N8N_WEBHOOK_SECRET unset, plain string compare otherwise) and the handler only acknowledges — no CRM/ticket side effects wired ('later slice'). |
| `n8n-outbound-trigger-adapter` | partial | `apps/web/src/server/trpc/automation-router.ts` | automation.* tRPC surface (health/listWorkflows/eventMap/propose/trigger) over packages/integrations/src/n8n/client.ts with a 6-event N8N_EVENT_MAP; production triggering HITL-gated behind N8N_ALLOW_PRODUCTION_TRIGGER and blocked on the hrmny tenant API key (readonly/degraded mode without it). |

## Non-tRPC HTTP handlers (from critique sweep)

These real route handlers are outside the tRPC surface and were absent from the api/jobs/integrations sweeps — surfaced by the critique pass. They carry live auth/webhook trust boundaries.

| Handler | Status | Path | Note |
|---------|--------|------|------|
| `api-scim-v2-endpoints` | real | `apps/web/src/app/api/scim/v2` | 9 SCIM 2.0 route files (Users, Groups, Schemas, ResourceTypes, ServiceProviderConfig + [id] variants) backed by 850-line server/scim.ts with bearer auth via enterprise-identity.ts; absent from the 638-item api sweep which counts only tRPC procedures (verified 637 .query/.mutation) |
| `api-mcp-work-endpoint` | real | `apps/web/src/app/api/mcp/work/route.ts` | 366-line MCP JSON-RPC endpoint exposing 11 work tools (list/create/update/complete/archive projects+tasks, comments) with scope-checked work-api token auth; in no sweep dimension |
| `api-work-v1-rest` | real | `apps/web/src/app/api/work/v1/[[...path]]/route.ts` | Public REST catch-all: 12 endpoints across GET/POST/PATCH/DELETE (projects, tasks, comments, complete, archive) with scoped token auth via work-api.ts (696 lines), delegating to tRPC caller; missing from api sweep |
| `api-work-sandbox-environment` | dev-only | `apps/web/src/app/api/work/sandbox/environment/route.ts` | Sandbox DB bootstrap/reset endpoint (bootstrapSandboxDatabase/resetSandboxDatabase) guarded by sandboxRequestAuthorized; destructive reset surface not in mock-surfaces sweep |
| `api-asana-webhook-receiver` | real | `apps/web/src/app/api/asana/webhooks/[token]/route.ts` | Token-addressed Asana webhook ingress delegating to server/asana-webhooks.ts (has tests); not listed under integrations or jobs sweeps |
| `api-n8n-webhook-receiver` | partial | `apps/web/src/app/api/webhooks/n8n/route.ts` | n8n callback ingress whose signature verification is an explicit stub: accepts unsigned requests when N8N_WEBHOOK_SECRET unset and uses non-constant-time compare; trust-boundary defect absent from defects sweep |
