# Failure ledger

Failures are retained as evidence. A later success does not erase the earlier boundary.

| Time (Asia/Karachi) | Slice                             | Failure                                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                              | Disposition                                                                                                                                                                                                                                        |
| ------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-28          | Production build                  | Both Next.js apps attempted to download Google fonts during `next build`; the Windows host returned `EADDRINUSE` while opening the font connection.                                                           | Build failed at `next/font/google` in both root layouts.                                                                                                                                                                              | Removed the build-time network dependency and used explicit local/system font stacks. The next full build passed 2/2 apps and generated 86/86 main-app pages.                                                                                      |
| 2026-08-28          | Browser repeatability             | A focused local Chromium rerun remained at `Checking access…`; the host had 14,796 `FIN_WAIT_2`, 14,342 `CLOSING`, and 454 `LAST_ACK` TCP rows, and new loopback requests returned socket-address exhaustion. | Playwright error contexts for both Sales Growth tests plus read-only `netstat` counts.                                                                                                                                                | Stopped only the two test servers started by this run. No process was force-terminated and no Windows TCP setting was changed. Fresh-runner CI and deployed browser acceptance are the authoritative follow-ups.                                   |
| 2026-08-28          | Production database preflight     | The local read-only PostgreSQL preflight could resolve the canonical Supabase pooler DNS name but the connection returned `EAI_AGAIN` while the host was socket-exhausted.                                    | Exact non-secret target metadata still resolved to project `klrugedztqxlvyghyzxs`; no SQL ran.                                                                                                                                        | Added a non-secret `integrationInbox` readiness signal. Production migration state must be read back from a fresh runner/provider surface before the one-person Apollo call.                                                                       |
| 2026-08-28          | Live Apollo free search           | The first production People Search returned the deterministic Apollo mock even though `/api/ready` reported an Apollo credential reference.                                                                   | The live Sales Growth receipt said `1 people found via Apollo mock`; code read only `APOLLO_API_KEY`, while production stores the accepted staff credential in Vault.                                                                 | Unified free search and the bounded one-person match on `resolveIntegrationApiKey`, preserving env-first/staff-vault/org-vault resolution and never returning the key to the client. Regression tests cover both official Apollo operations.       |
| 2026-08-28          | Live keyboard navigation          | The production header advertised `Ctrl K`, but pressing it on Sales Growth did not open search.                                                                                                               | Authenticated browser acceptance stayed on `/crm/hunt`; the header had a link but no keydown handler.                                                                                                                                 | Added one scoped `Ctrl/Meta + K` handler in the staff shell and a narrow-viewport E2E assertion that navigates to `/work/search` and returns to the Sales Growth loop.                                                                             |
| 2026-08-28          | Narrow Sales Growth viewport      | Fresh-runner Playwright found the CRM section bar widened the document to 447 px at a 390 px viewport.                                                                                                        | The E2E assertion measured `documentElement.scrollWidth = 447`; the section bar's later `overflow: visible` rule defeated its original horizontal containment.                                                                        | Kept the desktop popover behavior and changed the <=720 px section bar to a wrapping two-row grid with bounded children. Added zero-min-width boundaries to the shell, workspace, CRM page, and Sales Growth page.                                 |
| 2026-08-28          | Apollo search identity mapper     | Live zero-credit People Search returned five provider IDs, titles, and companies, but every review label was blank.                                                                                           | Apollo's official Search contract returns `first_name` plus `last_name_obfuscated`; the adapter only read the enrichment-only `name` field.                                                                                           | Added a bounded mapper that prefers `name` when present and otherwise joins only the documented first and obfuscated-last fields. It never guesses or de-obfuscates identity.                                                                      |
| 2026-08-28          | Authenticated Sales Growth search | The exact production UI reached live Apollo twice but returned zero candidates for the default retail sentence and the hospitality placeholder sentence.                                                      | The UI sent the whole natural-language sentence as `q_keywords` while separately forcing `person_locations=["United Arab Emirates"]`; Apollo's official tutorial instead sends job role and geography as distinct structured filters. | Replaced the ambiguous field with an explicit Job title, fixed UAE market, and optional keyword. After merge `5d441bae1445f8a7fc2c3796c4d64853bef20108`, the authenticated production UI returned eight obfuscated review candidates at 0 credits. |
| 2026-08-28          | Structured-search local E2E       | All three focused Playwright scenarios timed out in `page.goto("http://localhost:3000/crm/hunt")` before DOM content or a product assertion.                                                                  | The Windows host repeated its previously recorded local Next transport/socket-exhaustion behavior; the new structured-search unit contracts, both package typechecks, both lints, and the direct zero-credit provider call passed.    | Retained the failed receipt and made no OS/network change. Fresh CI runners and authenticated production browser readback remain authoritative for the UI regression.                                                                              |

No paid provider request, outreach send, destructive data action, or production database write occurred in any failed path above.
