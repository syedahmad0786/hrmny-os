# Failure ledger

Failures are retained as evidence. A later success does not erase the earlier boundary.

| Time (Asia/Karachi) | Slice                         | Failure                                                                                                                                                                                                       | Evidence                                                                                       | Disposition                                                                                                                                                                                                      |
| ------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-28          | Production build              | Both Next.js apps attempted to download Google fonts during `next build`; the Windows host returned `EADDRINUSE` while opening the font connection.                                                           | Build failed at `next/font/google` in both root layouts.                                       | Removed the build-time network dependency and used explicit local/system font stacks. The next full build passed 2/2 apps and generated 86/86 main-app pages.                                                    |
| 2026-08-28          | Browser repeatability         | A focused local Chromium rerun remained at `Checking access…`; the host had 14,796 `FIN_WAIT_2`, 14,342 `CLOSING`, and 454 `LAST_ACK` TCP rows, and new loopback requests returned socket-address exhaustion. | Playwright error contexts for both Sales Growth tests plus read-only `netstat` counts.         | Stopped only the two test servers started by this run. No process was force-terminated and no Windows TCP setting was changed. Fresh-runner CI and deployed browser acceptance are the authoritative follow-ups. |
| 2026-08-28          | Production database preflight | The local read-only PostgreSQL preflight could resolve the canonical Supabase pooler DNS name but the connection returned `EAI_AGAIN` while the host was socket-exhausted.                                    | Exact non-secret target metadata still resolved to project `klrugedztqxlvyghyzxs`; no SQL ran. | Added a non-secret `integrationInbox` readiness signal. Production migration state must be read back from a fresh runner/provider surface before the one-person Apollo call.                                     |

No paid provider request, outreach send, destructive data action, or production database write occurred in any failed path above.
