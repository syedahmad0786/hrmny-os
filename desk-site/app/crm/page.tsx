import { CrmApp } from "./crm-app";

export const metadata = {
  title: "hrmny OS — CRM",
  description: "Harmony CRM redesign on the live desk",
};

export default function CrmPage() {
  return <CrmApp />;
}
